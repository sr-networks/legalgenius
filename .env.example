# --- Clerk authentication (REQUIRED) ---
# Replace <your-subdomain> with your Clerk instance subdomain
# Example dev instance: https://<your-subdomain>.clerk.accounts.dev/.well-known/jwks.json
CLERK_ISSUER=https://<your-subdomain>.clerk.accounts.dev
CLERK_JWKS_URL=https://<your-subdomain>.clerk.accounts.dev/.well-known/jwks.json

# --- Database (optional; defaults to local SQLite file) ---
# SQLite (default)
DATABASE_URL=sqlite:///./legalgenius.db
# Example Postgres (production):
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/legalgenius

# --- API server (optional) ---
# Comma-separated list of allowed origins for CORS
API_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true
ADMIN_EMAILS=you@example.com

# --- LLM provider config (choose one) ---
# Nebius (default)
LLM_PROVIDER=nebius
NEBIUS_API_KEY=your-nebius-api-key
# Recommended default model:
NEBIUS_MODEL=zai-org/GLM-4.5
# Optional:
# NEBIUS_BASE_URL=https://api.studio.nebius.com/v1/

# OpenRouter
# LLM_PROVIDER=openrouter
# OPENROUTER_API_KEY=your-openrouter-api-key
# Optional model override (defaults exist in code):
# OPENROUTER_MODEL=anthropic/claude-sonnet-4
# Optional referer headers:
# OPENROUTER_SITE_URL=https://yourapp.example.com
# OPENROUTER_SITE_TITLE=LegalGenius

# Ollama (local)
# LLM_PROVIDER=ollama
# OLLAMA_MODEL=qwen3:4b
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_API_KEY=ollama

# --- Pricing for euro-based credits (per 1K tokens) ---
# Defaults used if not set
PRICE_IN_EUR_PER_1K=0.002
PRICE_OUT_EUR_PER_1K=0.006
